{
  "name": "cqrs-event-sourcing-presentation",
  "private": true,
  "scripts": {
    "serve": "cross-env PORT=8000 http-server src --port 8000",
    "serve:dist": "cross-env PORT=8000 http-server dist --port 8000",
    "build": "node scripts/build.js"
  },
  "dependencies": {},
  "devDependencies": {
    "cross-env": "^7.0.3",
    "http-server": "^0.13.0"
  }
}
